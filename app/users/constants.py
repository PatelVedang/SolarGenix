class UserResponseConstants:
    # fmt: off
    SUPERUSER_CAN_CREATE="Only superusers can create users."
    INVALID_FIELDS = "Invalid fields specified."
    INVALID_EXPORT_FORMAT = "Invalid export format. Choose from csv, json."
    EMAIL_ALREADY_EXISTS = "A user with this email already exists!"
    ERROR_GENERATING_PASS = "Error in generating password"
    ERROR_SENDING_EMAIL = "Error iN sending email"
        
    # fmt: on
